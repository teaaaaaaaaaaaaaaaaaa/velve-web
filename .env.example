# API base URL — endpoint the waiting list form posts to.
# Production points at the public Velve API (api.velveapp.com).
# For local dev override in .env.local with http://localhost:3000
VITE_API_URL=https://api.velveapp.com
